{
  "scripts": {
    "dev": "concurrently \"cd ./backend && npm run dev\" \"cd ./frontend && npm run dev\"",
    "build": "npm i vite @vitejs/plugin-react@3.1.0 typescript && cd frontend && npm i --save-dev @types/react-dom --legacy-peer-deps && tsc && vite build",
    "start": "cd backend && npm install && npm run server",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^3.1.0",
    "typescript": "^5.2.2",
    "vite": "^4.4.11"
  }
}
